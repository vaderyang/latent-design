---
import Base from "../../layouts/Base.astro";
import StreamingDemo from "../../components/StreamingDemo.tsx";
---

<Base title="流式 · Latent demo" description="LLM streaming 下的动效：流式呈现理解的形成，而不是 token 的河流。">
  <section class="wrap" style="padding-top:64px">
    <a class="demo-back" href="/">◂ Latent · 潜</a>
    <div class="kicker">应用 Demo · LLM Streaming</div>
    <h2>流式：呈现理解的形成，而非 token 的河流</h2>
    <p>
      多数流式 UI 把<strong>原始 token</strong> 直接刷在主舞台（打字机效果）——这恰是这套语言反对的"活动剧场"。潛 的做法：原始流退到<strong>外围</strong>（快、转瞬即逝、可审计），主舞台按<strong>有意义的提交</strong>更新，用已有的动效词汇承载流式动态。点 <strong>↻ 重放</strong> 观察。
    </p>

    <div class="comp-shell">
      <StreamingDemo client:visible />
    </div>

    <div class="anti" style="margin-top:26px">
      <div class="acard" style="border-left-color:var(--hypo)">
        <div class="an">答案位占位先行</div>
        <div class="ap">答案没出来时不能 answer-first——「我的判断」是占位槽（微光"正在判断…"），模型提交时 <em>Settle</em> 填入。生命周期 thinking → forming → settled。</div>
      </div>
      <div class="acard" style="border-left-color:var(--inflect)">
        <div class="an">动效词汇 = 流式动态</div>
        <div class="ap"><em>Surface</em> 新判断浮现 · <em>Sink</em> 某假设被推翻下沉 · <em>Pulse</em> "改主意"拐点闪现 · <em>Settle</em> 答案凝定。流式就是把这些状态迁移实时播放。</div>
      </div>
      <div class="acard" style="border-left-color:var(--grounded)">
        <div class="an">流事件，而非 raw text</div>
        <div class="ap">推荐流式<strong>事件/补丁</strong>（node.add · confidence.update · refute · inflection · outcome.settle），而非 raw token 或脆弱的 partial-JSON。事件天然映射 UI 迁移，且能区分"暂定"与"已提交"。</div>
      </div>
      <div class="acard" style="border-left-color:var(--ink-400)">
        <div class="an">稳，不抖</div>
        <div class="ap">对"理解"按语义单元防抖更新（非逐字），预留空间防回流（CLS），尊重 <span class="mono">prefers-reduced-motion</span>。原始流可以快，理解要稳。别把会被推翻的中间态过早渲染成"比较确定"。</div>
      </div>
    </div>
  </section>
</Base>
